version: 0.2

phases:
  install:
    commands:
      - echo "========== INSTALL PHASE =========="

  build:
    commands:
      - echo "========== BUILD STAGE =========="
      - ls
      - echo "Raw source version: $CODEBUILD_SOURCE_VERSION"
      - echo "Build Stage Branch Name: $(echo $CODEBUILD_SOURCE_VERSION | cut -d'/' -f3)"
      - echo "=================================="

  post_build:
    commands:
      - echo "========== TEST STAGE =========="
      - ls
      - echo "Test Stage Branch Name: $(echo $CODEBUILD_SOURCE_VERSION | cut -d'/' -f3)"
      - echo "================================="

  finally:
    commands:
      - echo "========== DEPLOY STAGE =========="
      - ls
      - echo "Deploy Stage Branch Name: $(echo $CODEBUILD_SOURCE_VERSION | cut -d'/' -f3)"
      - echo "===================================="